server:
  port: ${SERVER_PORT:8082}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartlingua?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:smartlingua}
    password: ${DB_PASSWORD:smartlingua}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/smartlingua

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
